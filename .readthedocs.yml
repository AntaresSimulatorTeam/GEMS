version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"
  commands:
    - pip install uv
    - uv sync --frozen --only-group doc
    - uv run mkdocs build --config-file mkdocs.yml --site-dir $READTHEDOCS_OUTPUT/html
